import React from 'react';
import "./Loading.css";

const Loading = () => {
    return (
        <div className="Loading">
            <div className="loading-bar"></div>
        </div>
    );
};

export default Loading;